# CREATE TABLE IF NOT EXISTS Ingredients(ID INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, price NUMERIC, unit TEXT)
# CREATE TABLE IF NOT EXISTS Donut_ing(ingredient_id INTEGER, donut_id INTEGER, PRIMARY KEY(ingredient_id, donut_id), FOREIGN KEY(ingredient_id) REFERENCES Ingredients(ID), FOREIGN KEY(donut_id) REFERENCES Donuts(ID))
# CREATE TABLE IF NOT EXISTS Donuts(ID INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, gluten BIT, price NUMERIC)
# CREATE TABLE IF NOT EXISTS Orders(ID INTEGER PRIMARY KEY AUTOINCREMENT, number INTEGER, customer_id INTEGER, FOREIGN KEY(customer_id) REFERENCES Customers(ID))
# CREATE TABLE IF NOT EXISTS Customers(ID INTEGER PRIMARY KEY AUTOINCREMENT, full_name TEXT)
# CREATE TABLE IF NOT EXISTS Order_items(order_id INTEGER, donut_id INTEGER, quantity INTEGER, PRIMARY KEY(order_id, donut_id), FOREIGN KEY(order_id) REFERENCES Orders(ID), FOREIGN KEY(donut_id) REFERENCES Donuts(ID))
