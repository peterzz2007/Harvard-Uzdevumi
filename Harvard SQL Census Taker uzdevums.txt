# CREATE VIEW rural AS SELECT * FROM census WHERE locality LIKE '%rural%'
# CREATE VIEW total AS SELECT SUM(families) as "families", SUM(households) as "households", SUM(population) as "population", SUM(male) as "male", SUM(female) as "female" FROM census
# CREATE VIEW by_district AS SELECT district, SUM(families) as "families", SUM(households) as "households", SUM(population) as "population", SUM(male) as "male", SUM(female) as "female" FROM census GROUP BY district
# CREATE VIEW most_populated AS SELECT district, SUM(families) as "families", SUM(households) as "households", SUM(population) as "population", SUM(male) as "male", SUM(female) as "female" FROM census GROUP BY district ORDER BY population DESC
